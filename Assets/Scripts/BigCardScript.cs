using UnityEngine;
using System.Collections;

public class BigCardScript : MonoBehaviour {

	// Use this for initialization
	void Start(){
		//set the type, orientation of each small card
//		var topCard = this.GetComponentInChildren<CardScript>();
//		var botCard = this.GetComponentInChildren<CardScript>();
		var topCard = this.transform.Find(
		//Debug.Log(topCard);
		//Object[] T
	}
	
	// Update is called once per frame
	void Update(){
	
	}
}
